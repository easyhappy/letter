<%= stylesheet_link_tag    'session', media: 'all', 'data-turbolinks-track' => true %>

<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <div class="login-panel panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title">请您先登录</h3>
        </div>
        <div class="panel-body">
          <%= form_for(resource, as: resource_name, url: admin_user_session_path) do |f| %>
            <div class="field form-group">
              <%= devise_error_messages! %>
            </div>
            <div class="field form-group">
              <%= f.label "用户名" %><br />
              <%= f.text_field :username, autofocus: true, class: "form-control" %>
            </div>

            <div class="field form-group">
              <%= f.label :password %><br />
              <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
            </div>

            <span class="pull-right">未注册, 请<%= link_to "注册", new_admin_user_registration_path %></span>
            <div class="actions">
              <%= f.submit "登录", class: "btn btn-lg btn-success btn-block" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>